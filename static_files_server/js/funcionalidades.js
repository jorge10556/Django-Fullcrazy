function confirmar_eliminar(ruta){
    console.log(ruta);
    if(confirm("¿Está seguro?")){
        location.href = ruta;
    }
}


function confirmar_salir(ruta){
    console.log(ruta);
    if(confirm("¿Está seguro?")){
        location.href = ruta;
    }
}